import { beforeEach, describe, expect, mock, test } from "bun:test";

beforeEach(() => {
	// xyz.mockClear();
});

describe("test suite", () => {
	test("test", () => {
		expect(true).toBe(true);
	});
});
